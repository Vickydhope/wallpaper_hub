/*import '../model/location.dart';
import '../model/review.dart';*/

class HeroTag {
  static String image(String urlImage) => urlImage;
  static String searchField() => "photo_search_field";

/*  static String addressLine1(Location location) =>
      location.title + location.addressLine1;

  static String addressLine2(Location location) =>
      location.title + location.addressLine2;

  static String stars(Location location) =>
      location.title + location.rating.toString();

  static String avatar(Review review, int position) {
    var tag = review.urlImage + position.toString();
    return tag;
  }*/
}
